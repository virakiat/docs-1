import { WIP } from '@/components/wip/WIP';

{/** @type {import('@/lib/mdxPageProps').MdxMetaProps} */}
export const meta = {
    description: '',
    emoji: '⚙️',
    contributors: [
        'luc.eth'
    ]
};

# Coin Types

The ENS protocol supports a variety of different coin types.
To ensure support for all of these, the protocol uses a standardized way to encode coin types.

<CoinTypeList />
